# ASL Detector — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────────────────
ultralytics>=8.1.0          # YOLOv8 training, evaluation, inference
torch>=2.0.0                # PyTorch (CPU or GPU)
torchvision>=0.15.0

# ── Hand detection / annotation ───────────────────────────────────────────────
mediapipe>=0.10.0           # Hand landmark detection for auto-annotation

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python>=4.8.0        # Webcam capture, drawing, image I/O

# ── Data science & plotting ───────────────────────────────────────────────────
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0             # Optional: nicer confusion matrix styling
pandas>=2.0.0               # Per-class CSV analysis
scikit-learn>=1.3.0         # Confusion matrix, classification report helpers

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0                # Progress bars
Pillow>=9.5.0               # Image loading / augmentation
pyyaml>=6.0                 # YAML config parsing
